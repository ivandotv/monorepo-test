flags:
  # filter the folder(s) you wish to measure by that flag
  faker:
    # only include files in the backend folder
    carryforward: true
    paths:
      - packages/faker/
  two:
    # only include files in the backend folder
    carryforward: true
    paths:
      - packages/two/
